class Solution {
  public:
    vector<int> kLargest(vector<int>& a, int k) {
        sort(a.rbegin(),a.rend());
        vector<int>ans;
        for(int i=0;i<k;i++)
            ans.push_back(a[i]);
            
        return ans;
    }
};
